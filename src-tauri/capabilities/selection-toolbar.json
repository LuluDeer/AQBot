{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "selection-toolbar",
  "description": "Minimal capability for the non-focusable text selection toolbar",
  "windows": ["selection-toolbar"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
